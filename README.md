# Dental Clinic Offline (Local PC Software)

Runs fully offline on one computer using Python + SQLite (no internet needed).

## Start

```powershell
python app.py
```

Open: `http://127.0.0.1:8000`



## Data

The database file is created next to `app.py`:

- `clinic.sqlite3`

You can copy this file for backup.
